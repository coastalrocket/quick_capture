Quick Capture QField Plugin

Intended for Big Button feature capture.

Long press settings button to change feature types and target layer.

If photos are enabled the camera interface is displayed. It's not possible to capture a photo without this interface displaying.

Requirements:
point layer contains 'type' text field, 'photo' attachment field.
also with adding a hidden timestamp field with now() as default value.
